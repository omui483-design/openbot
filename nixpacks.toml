[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm build",
  "pnpm ui:build"
]

[start]
cmd = "node dist/index.js gateway --port $PORT --bind 0.0.0.0"
